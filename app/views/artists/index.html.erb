<ul><% @artists.each do |artist| %></ul>
  <li><%= artist.name %> - <%= pluralize(artist.songs.count, 'song') %>
    <ul> 
      <% artist.songs.each do |song| %>
        <li><%= link_to song.title, song %></li>
      <% end %>

    </ul>
  </li>
<% end %>